import { StyleSheet, Text, View } from "react-native";
import React from "react";

import { styles } from "./styles";

interface OwnProps {}

const AddDriver: React.FC<OwnProps> = ({}) => {
  return (
    <View>
      <Text>AddDriver</Text>
    </View>
  );
};

export { AddDriver };
